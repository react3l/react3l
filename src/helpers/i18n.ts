/**
 * Translate marker, use to mark a string as translation key outside of React component
 *
 * @param {string} key
 * @return {string}
 */
export function translate(key: string): string {
  return key;
}
