# react3l-cli

This library was generated with [Nx](https://nx.dev).

```sh
Usage: react3l-cli [options] [command]

React3L cli

Options:
  -V, --version                        output the version number
  -s, --scss                           Add scss support
  -d, --dest <dest>                    Destination directory (default: ".")
  -s, --scss                           Add scss support
  -d, --dest <dest>                    Destination directory (default: ".")
  -d, --dest <dest>                    Destination directory (default: ".")
  -d, --dest <dest>                    Destination directory (default: ".")
  -d, --dest <dest>                    Destination directory (default: ".")
  -d, --dest <dest>                    Destination directory (default: ".")
  -a, --asset <assetDir>               Asset folder
  -s, --source <sourceDir>             Source folder
  -i, --input <inputPath>              Input path (default: "src/")
  -o, --output <outputPath>            Output path (default: "public/assets/i18n/")
  -p, --partials <partialPath>         Partial path (default: "public/assets/i18n/partials/")
  -ic, --include <include>             Include pattern (default: "\\.(js|jsx|ts|tsx)$")
  -ex, --exclude <exclude>             Exclude pattern (default: "\\.(spec|test)\\.(js|jsx|ts|tsx)$")
  -ks, --key-separator <keySeparator>  Key separator (default: ".")
  -is, --indent-size <indent>          Indent size (default: "2")
  -l, --languages <languages...>       Supported languages
  -m, --marker <marker>                Translate marker (default: "translate")
  -h, --help                           display help for command

Commands:
  generate <schematic> <name>          Generate component, service, repository and model
  g:c <name>                           Generate component (alias)
  g:m <name>                           Generate model (alias)
  g:r <name>                           Generate repository (alias)
  g:s <name>                           Generate service (alias)
  g:f <name>                           Generate filter (alias)
  clean <asset>                        Clean resources (asset files, scss classes)
  clean <scss> <path>                  Clean resources (asset files, scss classes)
  translate <action>                   action: extract | merge
  help [command]                       display help for command
✨  Done in 0.21s.
```
